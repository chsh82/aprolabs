EXTERNAL_API_KEY=

# 일반 어휘 퀴즈 모듈(app/vocabulary_quiz) - 실제 적재(--apply)는 이 값이
# local_rnd 또는 research일 때만 허용된다. production이거나 비어있으면 거부.
APP_ENV=local_rnd
# 연구용 서버 등에서는 배포 디렉터리 바깥의 영구 데이터 경로를 가리켜야 한다.
# 비워두면 로컬 저장소 안 기본 경로(data/vocab/vocabulary_quiz_rnd.db)로 폴백.
VOCABULARY_QUIZ_DB_PATH=
